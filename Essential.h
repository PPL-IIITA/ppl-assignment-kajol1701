#include <bits/stdc++.h>

using namespace std;

#define ll long long int

class Essential
{
	public:

    ll price = 0, value = 0;

    void printData()
    {
        printf("%lld ", price);
        printf("%lld\n", value);
    }
};
