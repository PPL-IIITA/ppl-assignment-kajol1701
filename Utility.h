#include <bits/stdc++.h>

using namespace std;

#define ll long long int

class Utility
{
	public:

    ll price = 0, value = 0, u_value = 0, u_class = 0;

    void printData()
    {
        printf("%lld ", price);
        printf("%lld ", value);
        printf("%lld ", u_value);
        printf("%lld\n", u_class);
    }

};
